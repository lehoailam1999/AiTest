QUY TẮC CHUNG UNIT (SPEED — BACKEND ONLY, PORTABLE):
1. Bám Knowledge/Freeze — không invent bucket rỗng; không gắn stack/domain mẫu; cấm invent class VI.
2. module = Feature; title VN `[Feature]-[Hành động BE]-[Kết quả]` (cấm Class.Method Latin · cấm form/popup/Bước/UI/ClientApp).
3. Steps/expected = mock → gọi SUT backend → assert return/exception; thiếu → [Giả định].
4. Không gộp Unit+E2E; không sinh TC UI/wizard. Ưu tiên cover BR+VALIDATION+ERROR trước pad FEATURES.
5. Tôn trọng SPEED MODE — nhánh backend chính; priority/severity thang Việt.
