# Tránh 5000/5001 (Forensic .NET). Desktop: VITE_API_URL=http://127.0.0.1:8000/api
# Cùng cổng với production API_PORT (không chạy Docker API + native cùng lúc).
PORT=8000
DATABASE_URL=host=localhost user=postgres password=postgres dbname=AITestDb port=5433 sslmode=disable TimeZone=UTC
JWT_KEY=AITest-Super-Secret-Key-Min-32-Chars-Long!
ENCRYPTION_KEY=AITest-Super-Secret-Key-Min-32-Chars-Long!
JWT_ISSUER=AITest.API
JWT_AUDIENCE=AITest.Client
JWT_ACCESS_HOURS=8
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4200,http://localhost:4300,tauri://localhost,http://tauri.localhost,https://tauri.localhost
