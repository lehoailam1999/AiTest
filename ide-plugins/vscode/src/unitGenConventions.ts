/**
 * Fallback when SUT `.ai-test/unit-conventions.md` is missing.
 * SoT text lives in `@aitest/ide-protocol` — do not fork wording here.
 */
export {
  UNIT_CONVENTIONS_CORE as EMBEDDED_UNIT_CONVENTIONS,
  UNIT_LAYOUT_RULE,
  UNIT_GEN_LIMITS,
} from "@aitest/ide-protocol";
